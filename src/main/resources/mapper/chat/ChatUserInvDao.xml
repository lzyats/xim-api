<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.modules.chat.dao.ChatUserInvDao">

    <!-- 字段集 -->
    <sql id="baseSql">
        select inid, user_id, user_inid, inv_usdt, create_time, deleted, status from chat_user_inv
    </sql>

    <!-- 查询列表字段 -->
    <select id="queryList" resultType="com.platform.modules.chat.domain.ChatUserInv">
        <include refid="baseSql"/>
        <where>
            <!--
            <if test="inid != null and inid != ''">
                and inid = #{inid}
            </if>
            -->
            <!--
            <if test="userId != null and userId != ''">
                and user_id = #{userId}
            </if>
            -->
            <!--
            <if test="userInid != null and userInid != ''">
                and user_inid = #{userInid}
            </if>
            -->
            <!--
            <if test="invUsdt != null and invUsdt != ''">
                and inv_level = #{invUsdt}
            </if>
            -->
            <!--
            <if test="createTime != null and createTime != ''">
                and create_time = #{createTime}
            </if>
            -->
            <!--
            <if test="deleted != null and deleted != ''">
                and deleted = #{deleted}
            </if>
            -->
            <!--
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
            -->
        </where>
    </select>

</mapper>
