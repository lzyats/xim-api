<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.modules.sys.dao.SysHtmlDao">

    <!-- 字段集 -->
    <sql id="baseSql">
        select id, html, ctime, roulekey, remake, url from sys_html
    </sql>

    <!-- 查询列表字段 -->
    <select id="queryList" resultType="com.platform.modules.sys.domain.SysHtml">
        <include refid="baseSql"/>
        <where>
            <!--
            <if test="id != null and id != ''">
                and id = #{id}
            </if>
            -->
            <!--
            <if test="html != null and html != ''">
                and html = #{html}
            </if>
            -->
            <!--
            <if test="ctime != null and ctime != ''">
                and ctime = #{ctime}
            </if>
            -->
            <!--
            <if test="roulekey != null and roulekey != ''">
                and roulekey = #{roulekey}
            </if>
            -->
            <!--
            <if test="remake != null and remake != ''">
                and remake = #{remake}
            </if>
            -->
        </where>
    </select>

</mapper>
